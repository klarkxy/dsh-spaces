# DSH Spaces Core

Shared isolation rules, registry domain operations, maintenance locking contract and recovery state machine. The desktop adapter and Host plugin consume the same source. Filesystem access, process management and Cordis/React integration belong to adapters.

Build from the repository root with `npm run build:spaces`. This package does not install or start DSH.
