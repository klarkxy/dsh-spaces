# 插件管理与 Space 导入导出实施计划

日期：2026-09-15。状态：方案已按用户后续确认收窄——插件/空间允许自行崩溃，Spaces 只反馈错误，不做装坏自动恢复。

代码基线：`main@1f339ac`。任务 01 已实测，见 [task-01-write-scope.md](task-01-write-scope.md)。该测量仍有效，但不再驱动本轮恢复模块。

## 1. 目标与已定边界

用户自行决定是否信任第三方插件。Spaces 负责记录安装与启动结果，并把失败、退出、崩溃的错误展示到对应空间和（能确定时）对应插件。不自动回滚安装环境，不把崩溃的空间重新拉起。

本计划承接「插件架构设计」讨论及用户随后确认的简化方向。2026-09-15 原方案中的安装前恢复点、失败自动恢复、重启后恢复、doctor 共用恢复记录，按用户确认移出本轮。

### 保留

- 全局插件库负责发现、下载和复用制品；真正安装、配置与运行属于具体 Space。
- 新空间模板只影响以后创建的空间；已有空间不自动继承变化。
- 插件包依赖由官方 CLI 所用的包管理器处理，运行依赖由 Cordis 处理。
- Spaces 保护自己的管理组件与隔离配置，提示已知不兼容组合；未知组合允许尝试，失败只报错。
- 分享包、同机克隆和整 Home 灾难恢复是不同操作。整 Home 快照仍是已有能力，不混进普通插件安装。
- 安装/启动/崩溃错误必须可见，且尽量落到具体插件，而不是只显示空间挂了。

### 本轮不做

- 普通插件安装/卸载的自动回滚、安装前恢复点、失败后撤销、重启后对账恢复、doctor 插件恢复入口。
- 通用「已安装但停用」的运行控制协议；现有入口若实际是卸载，文案说卸载。用户看完错误后自行卸载或不管。
- 插件权限沙箱、代码审计、发布者签名体系、分级信任平台。
- 通用 capability 求解、自动选择 provider、自动编译 replaces、穷尽动态冲突。
- 后台自动改回用户通过 CLI 修改的配置；启动时自动升级第三方插件。
- 跨空间全成功或全撤销事务、常规插件操作的整 Home 自动回滚。
- V1 导入覆盖/合并已有空间，或导入到 `web`、管理空间。
- V1 离线分享包、跨机器原样复制 `node_modules`、复制正在运行的进程状态。

基础包格式检查、解压目录限制、Host 管理入口保护继续保留。这些用于保证操作对象正确，不扩展成第三方插件安全承诺。

## 2. 错误反馈承诺

| 对象 | 处理 |
|---|---|
| 安装/卸载 CLI 失败、中断、读回失败 | 留下任务错误；写明空间、包名、请求版本、阶段和原始输出摘要。不回滚磁盘 |
| 空间启动失败、进程崩溃、非零退出 | 空间保持停止/崩溃。错误挂到该空间；能从 dump/日志/插件名对齐时再挂到具体插件 |
| 插件运行时报错但进程仍在 | 展示错误，不自动卸载、不自动重启 |
| 目标空间 sessions/storages、其他空间、凭据、运行时 | 不因报错而改动 |
| 用户下一步 | 已有卸载/停止入口。报错本身不是修复 |

不能确定是哪个插件时，允许只报到空间，并写明「未能归属到单个插件」，不得猜一个包名。

管理插件自升级继续走独立维护流程，不混入普通插件安装错误。

## 3. 已有实现与复用入口

以下为源码核查结果，不代表新能力已实现：

| 入口 | 当前能力与本次使用方式 |
|---|---|
| `src/main/plugin-ops.ts` | 插件增删、旧下载路径；旧 npm 下载仍取 latest，需要与工作台统一 |
| `src/adapters/node/workbench-maintenance.ts` | 精确版本下载、计划、逐空间操作、失败记录和读回；失败记录作为错误反馈来源，不扩展成回滚 |
| `src/main/diagnostics.ts` / 空间错误卡片 | 已有失败阶段、退出信息、有限日志；补齐插件归属，避免只显示笼统崩溃 |
| `src/main/snapshot-store.ts` | 整 Home 快照。本轮普通插件操作不调用 |
| `src/adapters/node/workbench-jobs.ts` | 持久任务与中断状态；任务失败即展示，不对账恢复环境 |
| `src/main/profile-registry.ts` | Profile 扫描 |
| `src/core/domain/isolation.ts` | 隔离字段读写 |
| `packages/doctor/src/recover.ts` | 整 Home/既有恢复入口。本轮不接普通插件安装 |

[换机与版本策略](../handoff-2026-09-14.md)仍有效。任务 01 测得普通 hoisted 安装可按单空间拷贝 `profiles/<space>/`；该结论留作以后若重开恢复功能的输入，本轮不实现恢复模块。

## 4. 最小实现约定

### 4.1 插件列表与下载

- 区分「插件库中的制品」「Profile 声明的包」「已配置的 bundle」；普通依赖不再伪装成可启用插件。
- `requestedSpec` 与实际 `resolvedVersion` 分开；未能读出实际版本就显示未知。
- UI 使用「已配置」与「空间启动检查结果」，不通过包名猜测所有运行实例均已正常。
- 现有“开关”实际执行安装/卸载的入口改成准确文案。本轮不新增保留安装但停用的协议。
- 支持同一包多个版本并存，缓存记录来源、版本和内容摘要。
- 继续支持已有手动来源。无法固定版本/制品的来源显示相应信息，不静默换成 latest。
- 已知冲突采用少量明确规则；只有同一实际作用域内的重名才可能构成冲突。

### 4.2 安装与错误

复用现有写控制者、维护互斥和持久任务，执行：

`预览目标 → 停止受影响进程（若在跑）→ 写入操作记录 → 安装/卸载 → 读回 → 成功或留下错误`

- 不在修改前做安装环境恢复点，失败不自动拷回 `profiles/<space>/` 或 pnpm store。
- 记录任务 ID、目标、阶段、CLI/进程退出和检查结果。以真实输出为准，不把「已安装」显示成「已运行」。
- 安装失败、启动失败、进程崩溃：保持失败现场，展示错误。不要为了验收而把空间重新启动成看起来正常。
- 中断后下次打开仍显示未完成任务和最后错误；不先静默对账再开放空间。
- 卸载仍走官方 `dsh plugin remove`。卸载失败同样只报错。

### 4.3 导入导出 V1

使用 `.dshspace` ZIP，格式尽量小：

```text
manifest.json       # 格式版本、来源环境、空间显示信息
plugins.json        # 插件来源、请求、实际版本、bundle 顺序
profile.patch.yml   # 可选的用户配置；只去除 Spaces 拥有的字段
assets/icon.png     # 可选
```

- V1 是在线重建空间的分享包，不保证不同机器得到完全相同的传递依赖树。
- 默认导出插件组成、顺序和显示信息。用户可选择附带配置；导出前提供配置内容预览。
- 不导出 Home 配置、共享凭据、聊天、存储和 node_modules。Host 组件不作为用户插件导出。
- V1 在线自动安装支持可解析的 npm 来源；Git、本机目录等已有来源列为待手动安装。不得静默丢弃。
- 导入只创建新的普通空间。临时空间显式标为未发布，并从普通扫描、启动和批量选择中排除。
- 发布失败或中断：清理本次未发布的新空间，留下任务错误。不能清理任何原有空间。不把发布中断做成插件环境回滚。
- 不支持项目不兼容的新格式时给出说明；读取包内配置不执行其中表达式。

### 4.4 批量与模板

- 批量按空间顺序运行同一单空间流程。前一个成功保留，后一个失败只停在后一个并展示该错误，不回滚前一个。
- 失败默认暂停剩余队列，保留「重试失败项」「继续剩余项」；取消只跳过尚未开始项。
- 共用下载结果，但不共用插件运行实例；不能重复执行已经成功的项目。
- 新空间模板复用导入/创建流程，是创建时的副本。修改模板不更新现有空间。
- 同机克隆不纳入 V1 完成门槛。

## 5. 分批任务

每个任务是一项独立交付。

| ID | 任务与结果 | 前置 | 预计修改入口 | 验收与验证 |
|---|---|---|---|---|
| 01 | 已完成：实测官方安装/卸载写入范围 | 无 | [证据](task-01-write-scope.md)、`scripts/verify-plugin-install-write-scope.mjs` | 两个空间不同版本；锁/依赖/链接；断网拷回仍能 dump-config。本轮不再据此做恢复产品 |
| 02 | 统一插件状态和精确版本下载，贯通工作台列表 | 01 | `plugin-ops.ts`、`plugin-library.ts`、`shared/types.ts`、工作台维护适配 | 指定旧版本不会下载 latest；同包多版本可区分；依赖不会显示为可启用插件；失败能指向包名 |
| 03 | 工作台/桌面展示安装、启动、崩溃错误；空间可保持崩溃 | 02 | 诊断/任务结果、插件列表、桌面控制适配、界面测试 | 安装失败和启动/崩溃失败都能看到空间+错误原文摘要；能确定插件时挂到该插件；不自动回滚、不把已安装当成已运行 |
| 07 | 导出普通空间的组成与可选配置 | 02 | 小型共享格式模块、导出模块、工作台导出入口、格式测试 | 精确记录可读版本与 bundle 顺序；配置默认不附带且可预览；未知来源明示 |
| 08 | 从包导入为未发布的新空间并完成发布/清理 | 07 | 导入模块、Profile 扫描、维护任务接线、导入测试 | 安装中空间不可见不可启动；最终名称启动成功才发布；失败清理未发布空间并留下错误；原有空间不变 |
| 09 | 补齐桌面导入导出入口及完整往返验收 | 03、08 | 桌面适配与 UI、工作台 UI、端到端验证脚本 | 导出→新 Home 导入→启动；部分导入列待处理项；失败与重名反馈正确 |
| 10 | 对多个普通空间批量安装/卸载，逐项报错 | 03 | 批量维护命令、持久队列、插件矩阵 UI、聚焦测试 | A 成功/B 失败时 A 保留、B 展示错误；取消和重启不重复 A；剩余项可继续 |
| 11 | 保存新空间模板并在创建时使用 | 09、10 | 模板数据、创建入口、模板 UI、聚焦测试 | 新空间获得模板副本；编辑模板不改变已有空间；创建失败清理未发布空间并报错 |

本轮取消（原恢复线，不再作为验收项）：

- 原 03 单空间安装环境恢复点
- 原 04 安装/卸载自动失败恢复
- 原 05 重启后恢复与 doctor 插件恢复入口
- 原 06 恢复结果/撤销入口（错误展示并入现 03）

### 里程碑

- **M1，任务 02–03：看清插件错误。** 空间可以崩；用户能看到是哪次安装或哪个空间/插件失败。
- **M2，任务 07–09：空间可分享、可导入。** 明确在线重建与手动待办；失败报错并清理未发布空间，不宣传精确复制运行环境。
- **M3，任务 10–11：批量应用和新空间模板。** 逐项结果，不增加跨空间回滚。

顺序：`01（已完成）→ 02 → 03 → M1`；`02 → 07 → 08 → 09 → M2`；`03 → 10`，`09+10 → 11 → M3`。

## 6. 验收方法与实施纪律

- 不把历史 rc.1/rc.2 验收记录当成新功能通过。不把任务 01 的离线拷回实验当成产品已提供恢复。
- 单元测试针对版本解析、状态读回、错误归属（有插件名 / 无法归属）。集成验证使用一次性 Home、两个空间、可控失败插件。
- 故障展示至少覆盖：安装失败、启动失败、进程崩溃、安装中退出。每项记录是否把错误展示给用户；不要求磁盘回到操作前。
- 测试插件只制造可控安装或启动故障；不用真实用户数据做故障注入。
- 复用现有诊断卡片、任务结果和 `lastError`；缺的是插件归属和「已配置 ≠ 已运行」，不是再做一套恢复框架。
- 每个里程碑补充本目录验收记录，更新任务清单。既有未完成项继续保留。
- 不提交、推送或发布，除非用户届时明确要求。

## 7. 下一项执行入口

从任务 02 开始：列表状态和精确版本，使后续错误能挂到具体包。任务 03 再把安装/启动/崩溃错误打到工作台和桌面。

任务状态统一维护在 [总任务清单](../todo.md) 的 2026-09-15 章节。本文件描述计划，不以文件存在表示任何验收已通过。
