# DSH Spaces 蓝图规范 v1

状态：v1 规范与使用指南。蓝图功能已随 [0.4.0](https://github.com/klarkxy/dsh-spaces/releases/tag/v0.4.0) 发布。验证结果与限制见[蓝图实施记录](tasks/blueprint-implementation.md)及[发布验收记录](tasks/release-0.4.0.md)。

日期：2026-09-21。规范标识：`dsh-blueprint`。JSON 格式版本：`1`。

蓝图描述一套插件组合及其配置预设。接收者粘贴分享码或打开 JSON，查看组成，填写本机选项，再据此创建一个新空间。蓝图作者可以从已有空间生成蓝图，也可以直接编写 JSON。

建议每位使用或开发 DSH Spaces 蓝图能力的人先阅读本页。用户从下面的操作说明开始；插件作者和工具开发者继续阅读字段定义、关系语义和验收场景。

本规范中的“必须”“不得”是实现要求；“建议”允许有理由的替代。已有 `.dshspace`、`SpaceRecipe` 和模板功能不因此成为本规范的实现。故障处理遵循 [let it crash](docs/let-it-crash.md)。

## 用户操作

以下入口位于当前工作区的共享工作台；已发布版本是否包含这些入口，以对应版本说明为准。

**使用别人分享的蓝图：** 在工作台打开“蓝图”，粘贴分享码或 JSON，也可以读取蓝图文件。选择“查看蓝图”，核对插件来源、版本、预设和已知组合问题。填写新空间名称及蓝图需要的本机选项，选择“预览创建”，确认后“创建空间”。完成后空间保持未启动，可从正常空间入口启动。

**分享自己的组合：** 从普通空间选择“生成蓝图”，勾选要分享的插件、配置预设和设置。查看无法分享的项目及原因；本机目录和模型应改成接收者输入。生成后可以“复制分享码”或“保存 JSON”。会话、附件和凭据不会因这一步被搬走。

分享码可以直接发在聊天消息、帖子或项目说明中，接收者不需要先解压文件。接收端仍需具备基础运行环境，并能够访问蓝图引用的插件来源。

## 开发者操作

1. 从[最小 JSON](docs/examples/blueprints/minimal.dsh-blueprint.json)开始；[写作样例](docs/examples/blueprints/writing.dsh-blueprint.json)展示较完整结构，但其中包名与配置是教学数据。
2. 发布符合官方 bundle 约定的插件，填入精确来源和版本；把希望参与运行的层按顺序列入 `profile.bundles`。
3. 填写可分享预设，使用 `inputs` / `bindings` 表达本机选择，并说明已知依赖和互斥。
4. 按本规范校验 JSON，再在隔离测试 Home 中走接收者的实际流程。记录实际验证结果后分发 JSON 或分享码。

格式与运行语义共同构成标准：JSON Schema 通过不能代替安装、组合或实际功能验证。后文给出确定的编码与错误规则，第三方工具可据此生成同一种蓝图。

工具开发者可使用仓库中的 [JSON Schema](schemas/blueprint-v1.schema.json) 做字段检查，再用运行时解析器校验跨字段关系、输入位置及编码规则。Schema 不是插件安全或运行兼容性的证明。

在编辑器中关联该 Schema 即可；不要把 `$schema` 加入蓝图根对象，v1 根字段是封闭的。代码入口为 `src/core/domain/blueprint.ts` 的 `parseBlueprint` / `bindBlueprintInputs` / `diagnoseBlueprint`，以及 `src/adapters/node/blueprint-codec.ts` 的 `decodeBlueprint` / `encodeBlueprint`。JSON 与分享码的解析都走同一份规则。仓库验收入口为 `npm run test:blueprints`。

## 1. 产品边界

蓝图分发的是环境配方。插件代码及随插件发布的主题、模板等资源，通过声明的包来源安装；小型提示词、规则和设置可以作为 JSON 配置值携带。

| 内容 | v1 的处理 |
| --- | --- |
| 包来源、选定版本、有序 bundle 列表 | 明确记录 |
| 插件配置、静态启用状态、用户选择的设置 | 作为预设记录 |
| 模型、目录等本机选择 | 声明输入，应用时绑定 |
| 依赖、互斥、顺序关系 | 声明并诊断，不自动修改组合 |
| 二进制、插件源码、任意文件树 | 由插件包分发，不嵌入蓝图 |
| API Key、Cookie、私钥、本机连接 ID | 不进入蓝图 |
| 会话、附件、数据库、缓存、运行状态 | 属于独立的数据导入导出链 |

蓝图不提供备份、数据恢复、离线环境镜像或整套依赖树的逐字节复现。数据导入导出的规范和界面另行设计，不能通过蓝图字段隐含开启。

v1 的应用目标是**新建的普通 Web 空间**。它不覆盖或合并已有空间，不修改 `web` 或管理空间。这里的 Web 指应用界面类型，不是名为 `web` 的目标 profile。

蓝图由 Spaces 解释和应用，插件仍使用官方 DSH bundle 机制。不得宣传官方 CLI 已能直接读取蓝图文件或分享码。

## 2. 两种分享形式

### 2.1 JSON 文件

推荐文件名为 `<名称>.dsh-blueprint.json`，编码为 UTF-8，无 BOM。内容必须是一个 JSON 对象，遵循第 3 节的字段定义。

JSON 不允许注释、尾逗号、重复键、非有限数字或未配对的 Unicode 代理项。解析器必须报告重复键，不能采用“最后一项胜出”。对象键顺序和格式化空白不影响含义；数组顺序有意义，不得自动排序。省略字段与显式 `null` 不等价，只有本规范允许的位置可以使用 `null`。

数值采用有限 IEEE 754 binary64 语义；解析结果为整数时必须落在 `[-9007199254740991, 9007199254740991]` 内，否则拒绝。小数按 binary64 的值解释，负零规范化为 `0`。需要精确大整数或十进制原文的插件应使用字符串。下文的数值无损指这个共同数值模型，不保证保留数字的原始书写形式。

### 2.2 分享码

分享码与 JSON 表达同一个对象，不依赖云端短链服务。v1 定义两种编码：

```text
DSHBP1:J:<payload>
DSHBP1:Z:<payload>
```

- `J`：UTF-8 JSON 字节直接进行无填充 Base64url 编码。
- `Z`：UTF-8 JSON 字节先进行 RFC 1951 raw DEFLATE 压缩，再进行无填充 Base64url 编码。不是 ZIP、gzip 或带 zlib 头的流。
- `DSHBP1` 和编码字母区分大小写。Base64url 使用 RFC 4648 §5 字母表，不带 `=`。
- 符合 v1 的读取器必须支持两种编码。生成器建议选取较短的一种。
- 读取器可去掉整条输入首尾的空格、Tab、CR、LF；不得删除 payload 中间字符。非法字符、非规范 Base64url 尾位、截断流、尾随压缩数据及无效 UTF-8 均报错。
- 解码后必须重新验证 JSON；分享码版本与内部 `formatVersion` 必须一致。不得尝试其它解码方式作为后备。

v1 限制：去掉首尾空白后的分享码最多 2 MiB ASCII 字节；JSON 文件或解压后的 JSON 最多 1 MiB；JSON 容器嵌套最多 64 层，根对象计第 1 层。解压必须按输出上限中止，不能先无限解压再检查长度。超限应报告实际限制，不能截断或丢字段。

JSON 与分享码往返必须保留所有值及数组顺序；不要求对象键顺序、JSON 排版或压缩字节一致。编码不提供保密、签名或作者身份认证。

## 3. 根对象

| 字段 | 类型 | 必需 | 含义 |
| --- | --- | --- | --- |
| `kind` | 字符串 | 是 | 固定为 `dsh-blueprint` |
| `formatVersion` | 整数 | 是 | 固定为 `1` |
| `metadata` | 对象 | 是 | 蓝图自身的信息 |
| `requirements` | 对象 | 否 | 作者声明的宿主要求 |
| `packages` | 数组 | 是 | 蓝图直接选定的包；可为空 |
| `profile` | 对象 | 是 | 基础界面、bundle 顺序、配置和设置预设 |
| `inputs` | 数组 | 否 | 应用时填写的本机输入；缺省为空 |
| `bindings` | 数组 | 否 | 将输入写入指定预设位置；缺省为空 |
| `relations` | 数组 | 否 | 蓝图作者声明的组合关系；缺省为空 |
| `testedWith` | 数组 | 否 | 作者报告的验证环境；缺省表示未提供 |
| `extensions` | 对象 | 否 | 不影响执行的扩展注释 |

除 `profile.patch`、`profile.settings` 内的插件数据以及 `extensions` 外，本规范的对象均为封闭字段集合，未知字段报错。扩展键应采用 `作者或组织.字段名`；读取器可忽略其展示，但再次保存必须保留内容。扩展不得增加安装、执行、权限或写入行为；需要新行为时升级格式。

### 3.1 `metadata`

| 字段 | 类型 | 规则 |
| --- | --- | --- |
| `name` | 字符串 | 必填，非空，用于展示，不直接作为文件路径或空间 ID |
| `version` | 字符串 | 必填，合法的精确 SemVer，表示蓝图发布版本 |
| `id` | 字符串 | 可选，作者维护的稳定标识，如 `example.writing`；不代表可信身份或全局登记 |
| `description` | 字符串 | 可选，纯文本 |
| `author` | 字符串 | 可选，作者自述 |
| `homepage` | 字符串 | 可选，HTTPS 页面链接；展示链接不触发下载或执行 |
| `license` | 字符串 | 可选，蓝图的许可证说明，不替代各插件许可证 |

蓝图格式版本、蓝图发布版本、插件版本和宿主版本各自独立。同一蓝图的新版不会自动修改已创建的空间。修改发布内容时作者应更新 `metadata.version`。

### 3.2 `requirements` 与 `testedWith`

`requirements` 支持可选的 `dsh`、`spaces`、`node` SemVer 范围，以及 `os`、`arch` 非空字符串数组。v1 的系统值为 `win32`、`darwin`、`linux`；架构值为 `x64`、`arm64`。字段省略表示作者未声明，不表示验证过所有环境。

范围采用 npm SemVer 语义，预发布版本不被普通稳定范围自动包含；实现不得私自启用“包含所有预发布版本”。作者应对验证过的预发布环境给出明确范围或精确版本。

`testedWith` 每项包含必填的 `dsh`、`spaces`、`node`（精确版本）、`os`、`arch`、`checkedAt`（带时区的 RFC 3339 时间）和 `checks`（非空数组）。`checks` 的值限于 `install`、`compose`、`start`、`interaction`，仅列实际完成并通过的检查；可选 `evidence` 为 HTTPS 证据链接。

这些是作者报告，读取器不得显示为 Spaces 认证。宿主范围不匹配或没有验证记录属于诊断信息，不构成永久版本白名单，不触发运行时升级或切换。实际不支持的蓝图语义则按第 9 节拒绝应用。

## 4. 包来源与版本

每个 `packages` 元素包含：

| 字段 | 类型 | 规则 |
| --- | --- | --- |
| `name` | 字符串 | 必填，合法 npm 包名；同一蓝图不得重复 |
| `version` | 字符串 | 必填，精确 SemVer，不接受范围、tag 或 `latest` |
| `source` | 对象 | 必填，见下表 |
| `integrity` | 字符串 | 可选，单个 `sha512-<标准 Base64>` 摘要；解码后必须为 64 字节 |

| `source.type` | 其它字段 | 语义 |
| --- | --- | --- |
| `npm` | 无 | 从接收者选择的 npm registry 获取指定包和精确版本 |
| `github` | `repository`、`commit` | 仓库为严格的 `owner/repo`，commit 为 40 位小写十六进制完整提交；根目录是安装包 |

v1 应用器必须支持 npm 来源；GitHub 来源是可选能力。不支持时必须在写入前指出具体包，不能改用同名 npm 包或转向最新分支。GitHub monorepo 子目录、任意 Git URL、HTTP tarball、`file:`、`link:`、npm alias 和 shell 命令不属于 v1 来源。

`integrity` 在 v1 仅适用于 npm 发布 tarball，校验实际取得的 tarball 字节；GitHub 来源不得填写，以免把源码归档、构建产物和安装包的摘要混为一谈。npm 包有可取得的发布摘要时，生成器建议记录。未记录应明示“未锁定制品摘要”，不能伪造或以包名版本代替。

安装必须核对取得的包名和版本；GitHub 来源还必须核对实际提交。摘要或身份不符就使该次安装失败，不换来源。registry、代理和认证使用接收者配置，蓝图不得携带 registry token 或修改 registry 设置。

普通代码依赖继续由包自身的 `dependencies`、`peerDependencies` 等 npm 元数据负责。蓝图列出直接选择，不展开另一套传递依赖求解规则；固定顶层版本也不承诺传递依赖、peer 选择或构建结果完全相同。

## 5. Profile 组合与预设

`profile` 支持以下字段：

| 字段 | 类型 | 规则 |
| --- | --- | --- |
| `base` | 字符串 | 必填；v1 固定为 `web`，创建新的普通 Web 空间 |
| `bundles` | 字符串数组 | 必填；蓝图选定的有序 bundle 包名，可为空 |
| `patch` | 数组 | 可选；JSON 可表达的官方 Cordis patch，缺省为 `[]` |
| `settings` | 对象 | 可选；按 settings namespace 组织的完整预设值，缺省为 `{}` |

### 5.1 安装、激活与顺序

`packages` 声明安装内容；`profile.bundles` 声明加入组合的层。bundle 名称必须唯一，且必须对应 `packages` 中的一项。安装了包但没有列入 `bundles`，只代表不由这份蓝图加入该包的 bundle 层；该包仍可能被其它插件作为代码依赖使用。

列入组合的包必须有有效的官方 `dsh.bundle.patch` 声明。不存在或无声明时报告组合错误，不能当作已成功启用。

不得按包名字母顺序或 `relations` 自动重排 `bundles`，不得因传递依赖具有 bundle 声明就自动激活它。若官方安装命令临时追加了层，应用器必须把最终层列表装配为蓝图指定的顺序；这属于正常安装阶段，失败时如实保留和报告当时状态。

包级激活与插件行级禁用不是同一件事。行级静态 `disabled` 保留在 patch 中。一个 bundle 可以贡献多行、不同作用域和不同服务；不得把它们推断成一个运行状态。

### 5.2 配置表达

`patch` 使用官方 patch 数据结构的 JSON 表达，应用器按选定运行时的官方语义生成 YAML；官方 patch 中的 `id`、`config`、`disabled` 和 `insert` 均沿用官方含义。本规范不另造配置深合并规则。后层对某一行 `config` 的替换可能要求作者重述完整配置，不能只填希望改变的一个字段。

| 形式或字段 | v1 语义 |
| --- | --- |
| `{ "id": "目标行", ... }` | 修改已有行；可选 `name` 用于核对该行模块，不是更换模块 |
| `{ "insert": [条目] }` | 在根列表末尾添加条目 |
| `{ "id": "目标组", "insert": [条目] }` | 在已有组的成员末尾添加条目；目标必须是组 |
| 新条目的 `id` / `name` | 均必填；模块名遵守下文的来源限制 |
| `config` | JSON 值，整体替换；组的配置是条目数组，按条目结构校验 |
| `group` / `disabled` | 静态布尔值或 `null`，保留官方清空语义，不接受表达式 |
| `inject` / `intercept` / `isolate` | 官方的 JSON 数据形式或 `null`；`isolate` 为服务到 `true` 或字符串标签的映射 |

上述行字段构成 v1 的封闭集合；`insert` 操作只接受可选的目标 `id` 和 `insert`。配置内部的业务字段不受这份行字段表限制。针对同一 `id` 的后续修改是有序操作，可以修改先前插入的行，不属于 JSON 重复键错误。组成员身份和启用状态也不能借第 6 节的配置值绑定改写。实际组合时，目标不存在、组类型不符或 `name` 不匹配须明确报告，不能把官方跳过的修改记为已生效。

v1 只携带 JSON 数据，不携带 `!!js`、YAML 自定义标签、函数、脚本钩子或需要求值的模板表达式。普通字符串即使以 `!!js` 开头，也必须作为字符串序列化，不能转成 YAML 标签。需要动态行为的作者应通过正式插件实现；蓝图不执行作者提供的安装脚本。

官方 Loader 还把带有 `__jsExpr` 属性的对象视为待求值表达式。因此 `profile.patch` 和 `profile.settings` 中也不得出现这种对象，无论其属性值是什么；仅拒绝 YAML 标签不足以满足纯数据约定。

patch 内的插件模块引用必须是包名或包导出的子路径，且能归属到选定包、其已解析的依赖或实际宿主包。不得引用绝对路径、相对源码目录或 `file:` URL。读取器不能确定实际依赖解析结果时显示待检查；安装后的组合检查仍无法解析则失败，不能从作者机器的路径补取代码。

官方 `cordis:` 内置模块引用按实际运行时提供的内置项核对；不得把未知内置名当作已经解析成功，也不得借包子路径中的 `..` 跳出包目录。

从现有空间生成时，所选配置含动态表达式或其它无法无损表达的结构，必须定位并报告；不得执行表达式、取一次运行值充数或静默删掉。用户可以明确取消选择该项，或者作者将该行为改由插件提供。

`settings` 只保存用户明确选择的、可分享的 namespace 预设。每个 namespace 的值按整体写入目标新空间；未列出的 namespace 不复制，也不从源 Home 补齐。不把整份 `settings.yaml` 原样扫描后打包，更不能用“未命中秘密正则”证明所有内容可公开。

`llm-pi-ai` 和 `agent-default-model` 的跨 Home 映射在 v1 通过第 6 节的模型绑定处理，不得直接在 `profile.settings` 中携带这两个 namespace 的源机器值。

### 5.3 宿主与 Spaces 的归属

官方基础层由目标运行时提供，其确切版本须在应用预览和结果中显示。`@deepseek-ai/dsh-base`、`@deepseek-ai/dsh-web-app` 不放入 `packages` 或用户 `bundles`；版本前提写在 `requirements`，验证记录写在 `testedWith`。

Spaces 的 Supervisor、管理插件、view-bridge、llm-bridge 及其身份、控制地址、路径和临时凭据由目标端管理，不作为蓝图用户插件分发。蓝图需要模型绑定时，接收端按现有受管流程提供相应桥接；未具备能力必须在应用前报告，不能偷偷忽略绑定。

应用器必须保留并验证目标空间的隔离和单写者约束。保护规则使用本项目的实际受管组件与配置路径，不能只凭字符串前缀猜测。来自蓝图或 Home 层的配置若使目标指向 `web`、其它空间数据或控制目录，应拒绝相应操作；不得静默重写作者配置后宣称按原蓝图创建。

## 6. 本机输入与绑定

输入声明只描述接收者需要提供什么，不携带作者机器上的实际绑定结果。

每项 `inputs` 包含必填的 `id`、`type`、`label`、`required`，可选 `description` 和 `default`。`id` 匹配 `[a-z][a-z0-9-]{0,63}` 且唯一；`required` 为布尔值。类型为：

| `type` | 本机结果 | 默认值 |
| --- | --- | --- |
| `string` | 字符串，不用于秘密 | 可选字符串 |
| `number` | JSON 有限数字 | 可选数字 |
| `boolean` | 布尔值 | 可选布尔值 |
| `directory` | 用户选择的本机目录 | 不允许预填作者路径 |
| `model` | 本 Home 的连接及其模型选择 | 不允许预填连接 ID、Key 或本机引用 |

每项 `bindings` 只有两个必填字段：`input`（引用输入 ID）与 `target`。输入必须至少被一个绑定使用，不允许引用不存在的输入。

`target` 为以下二者之一：

```json
{"kind":"value","pointer":"/profile/patch/0/config/outputDirectory"}
```

```json
{"kind":"default-model"}
```

`value.pointer` 遵循 RFC 6901，仅能指向 `/profile/settings` 内的值，或 `profile.patch` 中经官方结构识别的 `config` 内的值。它必须指向已有位置；不得指向整个 `settings`、整个 `config`、包来源、entry ID、包名、启用状态、bundle 列表或受管字段。不支持追加数组项、创建路径、字符串插值、表达式或命令拼接。

`value` 绑定位置初始值必须是 `null` 占位符；`default-model` 是专用操作，没有对应的 JSON 占位位置。`string`、`number`、`boolean`、`directory` 只能绑定到 `value`；`model` 只能绑定到 `default-model`，整份蓝图最多一个默认模型绑定。重复目标及祖先/子路径重叠目标均为格式错误。

值按原类型替换 `null`，不能先拼成 JSON/YAML 文本再解析。`directory` 使用接收者选择的路径，仅写入预览中明确展示的插件配置；它不授予应用器任意文件复制或执行权限，也不得指向受保护的 Home 数据或控制目录。

输入按“显式填写值 → 声明的 `default` → 缺失”解析，预览必须显示最终采用的值及其来源。默认值必须匹配输入类型，也可以满足 `required: true`；`directory`、`model` 不允许出现 `default` 字段。

必填输入最终缺失时可以保存、查看和分享蓝图，但不能开始应用。可选 `value` 输入缺失时，应用器从最终配置中删除它绑定的占位属性；数组元素不能采用这种省略规则，因此可选输入不得绑定数组元素。可选 `model` 缺失时不执行模型绑定，不为该项创建共享连接或桥接配置，并在预览中说明沿用新空间正常的默认行为。数字 `0`、布尔值 `false` 和空字符串是已填写值，不能按 truthy 判断缺失。

模型绑定交给 Spaces 现有连接域校验，必须选中实际存在且可用的模型。源 Home 的连接 ID 不可复用；本机绑定结果留在本地。重新分享时恢复输入声明与占位符，不把目录、连接 ID 或秘密写回蓝图。

为支持重新生成，应用器应复用现有空间/任务持久化机制保留原始蓝图及绑定位置与 entry/namespace 的归属证据，本机填写值分开保存。重新生成前核对当前位置仍属于原绑定；配置结构或对象身份已变化时报告需要重新选择，不能靠目录文本匹配猜测。缺少这份来源记录时也不得承诺自动还原输入，生成器必须让用户明确指定新的输入与位置。这个记录不建立发布源同步，也不产生第二套运行时配置。

非模型插件需要的秘密应通过该插件的正常本机配置入口设置，不能借 `string` 输入或 `default` 把秘密嵌进分享码。v1 不定义通用秘密注入语言。

## 7. 依赖、互斥和顺序诊断

每项 `relations` 必填 `type`、`from`、`to`、`reason`，可选 `fromVersion`、`toVersion`。`from`、`to` 是不同的合法 npm 包名；版本条件是 npm SemVer 范围，省略表示不限制版本。`from` 必须是本蓝图选择的包；`to` 可以是任意其它合法包名，不要求已被选定。`reason` 是面向用户的非空说明。

v1 可直接识别的宿主目标仅为 `@deepseek-ai/dsh-base` 和 `@deepseek-ai/dsh-web-app`，使用实际宿主版本及组合信息诊断。其它未列入 `packages` 的目标显示“未由蓝图选定”；有实际依赖或运行证据时再说明是否存在及参与运行，没有证据时保留未知。不得仅因本机某目录下装有同名包就视为运行依赖满足，也不为声明隐式增加安装项。

这些字段表示蓝图作者的声明，不向第三方插件的 package.json 写回。

| `type` | 含义 | 诊断 |
| --- | --- | --- |
| `requires` | `from` 参与运行时需要 `to` 参与运行，且版本满足条件 | 缺失、未激活、版本不符或状态未知 |
| `conflicts` | 两者同时参与运行且命中版本条件时存在已知问题 | 显示双方、范围和原因；互相声明可合并展示 |
| `integrates` | `to` 存在并满足条件时，`from` 提供可选联动 | 缺少不报失败；存在也不自动开启功能 |
| `after` | 两者都出现在用户 bundle 列表时，`from` 应位于 `to` 后面 | 违反顺序则报告，不自动排序；目标未参与组合则显示不适用 |

先判断 `fromVersion`；若不命中，该条关系不适用。`requires` 的 `toVersion` 是所需目标版本，`conflicts` 是产生冲突的目标版本，`integrates` 是支持联动的目标版本。`after` 只有双方版本条件都命中时生效。

选定、已安装、已列入 bundle 和运行中激活必须分别展示。静态检查通常只能知道前三者；没有运行证据时不得推断整个包的所有插件都已激活。未知版本不得按“不冲突”处理；缺少任何兼容声明不等于兼容。

循环依赖、相互矛盾的顺序声明和合法但不兼容的组合是可报告的组合问题，不自动修正，也不因为诊断结果禁止合法定义存在。包管理器或宿主实际拒绝安装/启动时，报告它们的真实失败。

运行时元数据可提供额外诊断。例如读取 Harmony 的 `dsh.plugin.compatibility` 时，必须标明来自该包、该版本的社区声明，保留其原有含义；它不是官方字段，也不是蓝图中的自动安装指令。元数据诊断不要求安装 Harmony。

entry ID 重复和同名服务只在相应作用域及证据足够时报告确定冲突；后层覆盖前层本身是官方组合机制。不得把所有覆盖判为错误，也不得把代码文本搜索结果当成实际服务注册证据。

## 8. 生成与发布

普通用户的入口为“生成蓝图”，输出 JSON 或分享码。生成器读取已安装包的实际身份、精确版本、用户 bundle 顺序，以及用户选择的 patch/settings 预设；不把运行时 dump 当作可以公开分享的全部配置。

生成前必须展示：选中的包和预设、本机输入、无法表达的动态配置、无法确定的来源，以及依赖 Home 层或本机环境的已知条件。选择了无法表达或来源不明的内容时，不得静默删项后生成“完整蓝图”；用户可明确调整选择，再生成一份新的配方。

只有能够证明属于 Spaces 装配的字段和组件，才能在生成时转换为本规范的宿主要求或本机绑定。其它配置中的绝对路径不能全局搜索替换成占位符。

开发者制作蓝图时，应使用如下流程：

1. 将插件按官方 npm/bundle 约定发布，并保证最终制品包含所需运行文件及资源。
2. 编写或生成蓝图，选定精确来源，明确 bundle 顺序、配置预设和关系声明。
3. 将机器相关值改为输入；审阅所有将公开的文本和配置。
4. 在隔离的测试 Home 内应用最终 JSON/分享码，再验证组合、启动和实际功能；只记录通过的检查。
5. 发布 JSON 或分享码，并说明接收者仍需配置的本机条件。

作者应优先发布预构建的运行包。蓝图不能携带通用 `install.sh`、`postApply` 或 shell 参数作为应用步骤；第三方包自身的生命周期脚本仍须在实际安装前纳入来源与执行风险展示，不能将它们伪装成纯 JSON 操作。

## 9. 接收与应用流程

### 9.1 读取与预览

用户粘贴分享码或打开 JSON 后，先完成解码、结构校验和关系检查，展示包来源、版本、组合顺序、预设、本机输入、宿主实际版本和已知问题。

纯内容预览不得安装包、执行插件、求值配置、创建空间或修改 Home。元数据查询可作为明确可见的只读检查；查询失败须显示未知，不能假定包存在或兼容。仅为查看蓝图，不调用可能加载第三方代码的运行时命令。

| 发现的问题 | 行为 |
| --- | --- |
| 编码损坏、未知格式、无效字段、重复键、超限 | 拒绝解析或应用，保留原输入 |
| 摘要/身份不符、受保护目标、越权路径、已知秘密 | 拒绝相应操作，不修补或换来源 |
| 接收端不支持来源、patch 语义或绑定能力 | 写入前报告具体不支持项；可查看，不能假装已应用 |
| 必填输入尚未填写 | 保持预览，填写后才能应用 |
| 版本范围不符、运行依赖缺失、互斥、顺序矛盾 | 展示诊断；不替用户调整，不禁止合法空间定义存在 |
| 没有验证记录、缺少关系声明、运行状态未知 | 明确证据范围，不显示“已验证兼容” |

### 9.2 用户确认后的执行

应用流程使用现有 Supervisor、写入保护和任务机制，不另建蓝图后台或任务恢复服务。

1. 固定本次蓝图内容、本机输入、目标新空间和安装计划；进入单写者边界后再次检查目标及输入引用。预览与执行之间内容或受管状态变化时，拒绝旧请求并说明原因，不能悄悄应用新计划。
2. 准备本次配置和目标定义，校验结构、隔离及受管边界。可在未发布的暂存位置工作，但不能为了检查而提前执行插件。
3. 创建新空间，按计划安装所选包。蓝图顶层包按 `packages` 的顺序执行，底层包管理器内部解析不由该顺序规定。首个安装失败即终止该次应用，剩余项不执行。
4. 按 `profile.bundles` 装配最终顺序，写入绑定后的 patch 和 settings，并完成隔离检查。一次配置文件写入保持原子性；不宣称整个多文件过程具有不存在的事务能力。
5. 记录结果。蓝图应用不自动启动空间；用户从正常管理入口启动。

若正常空间创建流程包含必要的官方基础包安装，它必须在计划及结果中显示；基础包或桥接安装失败同样终止，不因归属宿主而隐藏。

安装插件会执行第三方代码的事实必须在安装确认中明确。JSON 的结构校验、摘要校验和元数据声明都不证明插件安全；不得把来源声明当作权限授权。现有的认证与执行批准机制继续适用。

### 9.3 结果与失败

结果至少分开记录四个阶段：空间创建、插件安装、预设写入、空间启动。前三项分别为 `succeeded`、`failed` 或 `not-run`；蓝图应用返回时启动固定为 `not-run`。整体应用成功要求前三项均成功，不能把“空间目录已经出现”写成“蓝图应用成功”。

结果须包含目标空间（如已创建）、失败阶段、实际已安装和未执行的顶层包、已完成的配置写入，以及已知原因或明确未知。每个顶层包的完成状态只代表该包实际安装完成；不能据此推断其插件已激活。

发布前只清理本次能够证明归属的临时资源。空间或配置发布后失败，保留真实现场和已完成改变；不回滚、不自动禁用插件、不重试、不续跑。重新打开界面只读回原任务；中断且结果无法确认时明确记录，不重新执行命令。

以后用户主动修改或启动空间属于正常管理操作，不把错误页面包装成恢复向导。已应用蓝图不与发布源保持自动同步；再次应用同一蓝图创建另一个新空间。

## 10. 示例

最小蓝图只有基础界面，没有额外包：

```json
{
  "kind": "dsh-blueprint",
  "formatVersion": 1,
  "metadata": { "name": "空白 Web 工作台", "version": "1.0.0" },
  "packages": [],
  "profile": { "base": "web", "bundles": [] }
}
```

这个示例仍需要接收端安装并提供正常的 DSH/Spaces 基础环境，不代表断网可用。

可直接读取的文档样例：

- [minimal.dsh-blueprint.json](docs/examples/blueprints/minimal.dsh-blueprint.json)：上述最小对象。
- [minimal.share.txt](docs/examples/blueprints/minimal.share.txt)：同一对象的真实 `J` 编码，供解码往返核对。
- [writing.dsh-blueprint.json](docs/examples/blueprints/writing.dsh-blueprint.json)：主题选择、顺序、配置、本机目录和模型绑定的完整示意。包名、版本和插件配置为虚构教学数据，不是可安装产品或兼容验证记录。
- [zhuqing.dsh-blueprint.json](docs/examples/blueprints/zhuqing.dsh-blueprint.json)：引用已发布的 `dsh-theme-plugin@0.3.3`，并记录实际制品的 SHA-512 摘要，可用于验证联网安装。实际功能验证范围见[实施记录](tasks/blueprint-implementation.md)；这里不添加未经验证的 `testedWith`。

写作样例同时列出两个主题包，只有纸张主题进入 `bundles`；运行依赖明确指向该主题。用户选定输出目录和模型后创建新的写作空间。替代主题的互斥声明可保留，但未参与组合不等于当前就发生冲突。

## 11. 版本演进与旧功能

改变字段含义、增加执行能力或引入读取器不能安全忽略的行为，必须提升 `formatVersion` 并定义对应分享码版本。未知版本只报告不支持，不猜测旧格式。

现有 `.dshspace` ZIP、模板和 `SpaceRecipe` 保持原有身份；不得仅改扩展名或填入 `formatVersion: 1` 就当成蓝图。未来转换入口必须重新检查来源、版本、动态配置、受管组件和本机值；丢失信息要展示，不能静默转换。

当前实现入口如下。蓝图复用既有任务、写入锁和官方安装路径；旧 ZIP 分享、recipe 和模板继续走各自入口。

| 入口 | 职责 |
| --- | --- |
| `src/core/domain/blueprint.ts` | 格式检查、输入绑定和声明关系诊断 |
| `src/adapters/node/blueprint-codec.ts` | JSON 与 J/Z 分享码读取、编码及大小限制 |
| `src/adapters/node/blueprint-package.ts` | npm 制品下载、缓存字节、身份和摘要检查 |
| `src/adapters/node/blueprint-runtime.ts` | 从实际绑定的 DSH 读取宿主、bundle、组合和模块解析证据 |
| `src/adapters/node/blueprint-workbench.ts` | 来源选择、生成、预览、一次性计划和分阶段应用 |
| `src/shared/workbench-blueprint.ts` | 工作台蓝图请求、预览和结果类型 |
| `packages/plugin/src/workbench/blueprint-ui.tsx` | 桌面与浏览器共用的蓝图界面 |

工作台增加 `blueprint.source`、`blueprint.generate`、`blueprint.inspect`、`blueprint.preview` 查询，以及 `blueprint.apply` 任务命令。预览计划保存在当前服务内存中，绑定服务 epoch、状态版本和输入观察；过期、被消费或服务结束后不能应用。现有鉴权、请求/响应校验和单写者机制继续约束这些入口。

## 12. 验收场景

| 场景 | 必须观察到的结果 |
| --- | --- |
| 同一蓝图以 JSON、J 码、Z 码读取 | 解码对象一致，中文、字符串、数值及数组顺序无损 |
| 损坏编码、重复键、解压超限或未知版本 | 读取失败，无安装和 Home 写入 |
| 两个主题均安装，仅一个参与组合 | 安装清单保留二者，最终 bundle 列表仅激活所选层 |
| 顺序改变会覆盖配置 | 原顺序被保留，whole-config 替换按官方语义生效 |
| 无效 bundle 声明或安装身份不符 | 对应阶段失败，不能报告已启用或替换来源 |
| 输入值为 false、0、空字符串，或含引号与换行 | 作为原类型值绑定，不误判缺失，不产生额外 YAML/命令 |
| 声明默认值，又显式填写 false、0 或空字符串 | 显式值优先；未填写时才采用类型正确的默认值 |
| 可选模型未选择 | 不新增模型绑定或共享连接授权；预览说明正常默认行为 |
| 出现超安全整数、非有限结果或负零 | 前两者拒绝；负零按规范转为 0，不静默舍入大整数 |
| 缺少必填输入、绑定不存在路径或重叠路径 | 应用前报告，空间未创建 |
| 在两个 Home 应用同一模型蓝图 | 分别选择本地连接，分享码中不出现任一 Home 的凭据或连接 ID |
| 来源空间使用 settings、动态 patch 和本地插件 | 可分享部分准确列出；不支持部分定位说明，不能静默遗失 |
| 应用后重新生成蓝图，或绑定对应结构已被修改 | 前者按来源记录还原输入，后者明确报告重新选择，不暴露本机值 |
| 合法但有互斥或依赖诊断的蓝图 | 定义可存在，不自动调版本、补启用、重排或禁用 |
| A 包已安装、B 安装失败、C 尚未执行 | A 保留，B 失败，C not-run，无回滚、续跑或自动启动 |
| 空间已创建，随后配置写入失败 | 创建成功、配置失败、整体应用失败；实际改变如实保留 |
| 预览后蓝图内容或目标受管状态变化 | 旧请求拒绝，不应用与预览不同的内容 |
| 分享码含秘密、受保护路径或控制组件替换 | 拒绝相应操作，不泄露秘密到日志 |
| 生成及应用蓝图前后比较保护区 | `web`、其它空间和默认 sessions/storages 不被改写 |
| 用最终发布制品在干净测试 Home 创建 | 接收者无需手工修改 profile 内部文件；作者实际功能可独立验收 |

这些场景定义验收要求，逐项证据与未验证范围见[实施记录](tasks/blueprint-implementation.md)。文档样例解析和编码往返不等于安装、启动或真实用户流程通过。

## 13. 依据

以下官方与社区资料是本次设计的参考，不把上游 master 的描述当成本项目运行时已支持的能力：

- [官方：打包与安装插件](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/user/develop/basic/publish.zh.md)：bundle/profile 分工、层顺序及配置覆盖。
- [官方：package-manifest](https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/util/package-manifest/README.md)：manifest 与宿主要求声明；声明不等于强制执行或兼容验证。
- [Harmony：插件兼容声明](https://github.com/memorax-ai/dsh-harmony#plugin-compatibility)：包名与版本范围形式的 requires/conflicts/integrates，作为诊断而非自动安装。
- [dsh-composition-check](https://github.com/aryswisnu/dsh-composition-check)：组合检查需要区分静态证据与实际运行证据。
- [RFC 4648](https://www.rfc-editor.org/rfc/rfc4648)：Base64url 编码。
- [RFC 1951](https://www.rfc-editor.org/rfc/rfc1951)：raw DEFLATE。
- [RFC 6901](https://www.rfc-editor.org/rfc/rfc6901)：JSON Pointer。

上游资料提供设计背景。当前实现另外针对本机选定的官方运行时验证 patch、模块解析和安装行为；具体版本、真实流程结果及证据限制见[实施记录](tasks/blueprint-implementation.md)，不从上游文档推定兼容性。
